# Cổng chạy ứng dụng
PORT=5000

# Cấu hình MySQL (Sử dụng thông tin mặc định của XAMPP)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=clinic_db

# Khóa bí mật cho JWT (ĐỔI QUAN TRỌNG: thay thế bằng một chuỗi dài và ngẫu nhiên)
JWT_SECRET=YOUR_VERY_STRONG_AND_RANDOM_SECRET_KEY